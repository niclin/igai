<%= simple_form_for [:account, @product] do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :price %>
  <%= f.input :pictures, as: :file, input_html: { multiple: true } %>
  <%= f.button :submit %>
<% end %>
